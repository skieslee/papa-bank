# Real Firebase config (keep your keys out of the repo).
# Copy firebase-config.example.js to firebase-config.js and fill it in.
firebase-config.js

# Firebase deploy cache
.firebase/
*.log

# Dependencies (if ever used)
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
