// ===== Firebase config (template) =====
// Copy this file to "firebase-config.js" and fill in your own values.
//
// Leave the values empty to run in Local mode (data stays in this
// browser only; no setup required).
//
// For multi-device sync, create a free Firebase project at
// https://console.firebase.google.com, then under
// "Project settings > General > Your apps (Web)" copy the config here.
// See README.md for the full steps.

var firebaseConfig = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  appId: ""
};

// One shared dataset per family; usually no need to change this.
var BANK_ID = "family";
